<Window x:Class="IT_HelpDesk.Pages._General.SetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IT_HelpDesk.Pages._General"
        mc:Ignorable="d"
        Title="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_Title]}" Height="275" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_Instruction]}" Grid.Row="0" Margin="0,0,0,15" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_NewPasswordLabel]}" Width="100" VerticalAlignment="Center"/>
            <PasswordBox x:Name="NewPasswordBox" MaxLength="16" Width="200" PasswordChar="*"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_ConfirmLabel]}" Width="100" VerticalAlignment="Center"/>
            <PasswordBox x:Name="ConfirmPasswordBox" MaxLength="16" Width="200" PasswordChar="*"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_SaveButton]}" Width="80" Height="25" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button Content="{Binding Source={StaticResource LocalizationManager}, Path=[SetPassword_ExitButton]}" Width="80" Height="25" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
